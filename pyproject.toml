[project]
name = "dbt-contracts-demo"
version = "0.1.0"
description = "Hands-on demo for dbt contracts: model contracts, constraints, and versioning."
requires-python = ">=3.11"
dependencies = [
    "dbt-duckdb>=1.8.0",
]
